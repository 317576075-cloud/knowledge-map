---
name: knowledge-map-public
description: 高手学新东西，第一步不是开始学，而是先画地图。这个 skill 在你进入新领域时先查证权威资料，帮你画出这个领域的整体地图——由哪几个模块组成、彼此什么关系、提取核心概念，先学后学什么。再附一张可点开的美观知识地图（HTML）和对口书单帮你校准。适用于了解任何领域，例如职场、专业、技能、爱好、人物、文化等。当用户说：帮我拆解XX领域、XX领域全貌或知识框架、快速了解XX、画一张XX的知识地图、刚接触XX该从哪入手，及类似表达时调用。
---

# 领域知识地图

高手学新东西，第一步不是"开始学"，而是先画地图。这个 skill 在你进入新领域时先做一件事：查证权威资料，帮你画出这个领域的整体地图——由哪几个模块组成、彼此什么关系、哪些必须先学、哪些可以放后。让接下来的每一步，你都知道自己在哪。

## 怎么触发

用户说「帮我拆解XX领域」「XX领域的全貌或知识框架」「快速了解XX」「画一张XX的知识地图」「刚接触XX该从哪入手」及类似表达时，必须执行本流程，禁止绕过本 skill 直接凭模型知识开讲。宁可多问一句定用途，不可直接作答。

## 它会怎么做

1. **定用途**：先问清用途和基础——用途决定地图的详略和主干取舍，基础决定概念深度。没说清就追问，拒绝直接开画。
2. **研究检索**：查证至少 3 个独立权威来源（经典书目/课程大纲/官方文档/行业标准），附信息源清单，不凭印象瞎编，查不到的标"待验证"。
3. **拆解结构**：列出 3-7 个核心模块与关系，区分「先学这些」（绕不开的先修主干）和「有空再学」（可后置的分支）。
4. **提取概念**：每个主干模块提 3-5 个关键概念，全配大白话，术语一句内必给解释。
5. **来龙去脉**：补领域的演进脉络、当下流派、研究与验证方式。
6. **出图与书单**：对话给出文字版地图 + 入门路径 + 对口推荐书单（查证过、附链接、标验证状态），并生成一张美观的 HTML 知识地图（封面卡片 + 点按浮窗）。

## 输出风格

- **说人话**：像跟同事/朋友讲事情一样输出，有人味，禁止工序黑话和术语堆砌。步骤名是给自己看的工序，不要把"第X步""交付"这类词贴到用户脸上。
- **有温度**：像一个靠谱又有热情的朋友在帮忙，专业但不冷冰冰。用户主动来学新领域是迈出了一步，值得被认真对待。
- **措辞跟着领域语境走**：职场/专业领域用"步骤、模块、先修、后置"等中性职业用语，不强行比喻；生活化领域（如健身、育儿、旅行）才用贴合该场景的大白话比喻。概念解释始终要通俗，但比喻的"皮肤"必须跟场合匹配。
- **文字回复是主交付**：对话中必须给出完整的文字版内容（地图结构、概念、路径、推荐书单、信息源），HTML 是锦上添花的可视化附件，绝不"只发文件不说话"。IM 场景尤其如此——用户先读文字，附件随意点开。
- **IM 单消息原则**：在即时通讯场景，文字版和 HTML 链接必须出现在同一条回复里——文字版在前、链接收尾；不要分成多条消息发送，避免文字版被后续动作顶出用户视野。
- **HTML 交付契约（硬）**：HTML 地图必须以「附件」形态交付——IM 场景用附件能力和文字版同一条消息发出；严禁把本地路径当文本贴给用户（"MEDIA:/mnt/..." 这类路径文本等于没交付）。运行环境支持页面托管（如 NoCode）时，可额外附一个可点开的链接作为增强，但「文字版 + HTML 附件」是不可降级的底线。
- **先学这些/有空再学必须讲透**：输出地图时先用一句大白话讲清两者关系和切分理由（按第1步的用途）。

## 核心原则

1. **先骨架后血肉**：没有地图不扎进细节。
2. **关系大于罗列**：地图画的是谁依赖谁、谁驱动谁，不是知识点清单。
3. **检索先于发挥**：一切结构判断必须经权威资料核实，模型存量知识只能当草稿。宁可标注"待验证"，严禁编造。

## 工作流程

### 1. 定用途（硬门槛，显性交互）

先明确两点再往下走：**用途**（学这个领域为了什么场景/项目，决定地图详略和主干取舍）和**基础**（零基础还是有相邻经验，决定概念深度）。
- 用户已说明用途：复述确认即可继续。
- 没说明：提问并等待回答。**提问时必须附"为什么问"**：用大白话告诉用户不同答案会怎样改变地图的切法（如"A 场景主干切在日常动作，B 场景切在协作接口——同一个领域，两张不同的图"），让用户理解回答的价值，并预期地图是场景定制的，不是标准答案。**选项和后果必须对齐**：列了几个选项，就要展示几种切法差异（或明确归组说明），不得只展示部分选项的差异后将其余选项晾在一边。用户明确说"直接开始"时，给出合理默认场景并显著声明假设。
- 禁止跳过此步直接开画。

### 2. 研究检索（强制步）

拆解之前必须检索，禁止纯凭模型知识开画：
- 至少 3 个独立权威来源，优先级：经典著作/教材的目录与框架 > 名校公开课/官方课程大纲 > 官方文档/行业标准 > 高质量深度文章。
- 产出信息源清单（标题+链接），交付时在对话中附上（HTML 底部可附简版）。
- 模型存量知识只能当初稿：未被检索来源支撑的结构/概念，标注"待验证"。严禁编造来源、概念、数据、引文。

### 3. 拆解领域结构

- 基于检索结果列出 3-7 个核心模块，说清模块间关系（依赖、并列、上下游）。
- 标注主干 vs 分支：主干 = 其他模块反复依赖、绕不开的底层模块（多来源高频 + 被依赖）；分支 = 可后置的专项。
- 主干/分支的切分受第1步"用途"影响，需用大白话说明切分理由。

### 4. 提取关键概念

- 每个主干模块下提取 3-5 个关键概念（分支模块不展开）。
- 每个概念配一句大白话，禁止术语裸奔。

### 5. 来龙去脉

- **四个视角**检查缺角，缺哪个补哪个（各 1-2 条）：构成（由什么组成）/ 变化（怎么运转演变）/ 认识（怎么研究实践）/ 验证（怎么判断对错好坏）。
- **检查结果要看得见，但工序名不许出门**：四个视角都要有真实着落——构成 = 地图本体；变化 =「纵向看演进」块；认识 = 独立小块（这个领域怎么研究和实践：研究范式 + 干活方式），不得只在指引行里塞个文本片段；与横向流派（当下有哪些阵营/路线）严格分工——阵营归流派、方法归认识，同一条目不许两边都出现；验证 =「评价标准」块。补盲区开头可以加一句大白话说明这块是干什么的（如"地图之外，再补三块背景：它怎么演变来的、现在有哪些玩法、做得好不好怎么判断"），禁止把"四视角查缺角"这类工序黑话贴给用户。
- **纵横扫描**加深判断：纵向看演进——领域从诞生到现在的 2-3 个关键节点；横向看流派——当下主要流派/框架/工具的差异对比（2-3 项）。领域太新无演进可写时注明。

### 6. 出图与校准

**先在对话里给出文字版**（封面级，保持轻量）：
1. **地图文字版**：模块结构 + 关系 + 每个模块一句话 + 概念标签 + 先学这些/有空再学标注——像 HTML 的封面，让读者不看附件也能掌握全貌。**展开级的概念详释只放 HTML 浮窗**，不在对话里全文堆叠，避免文字版变成复读。
2. **入门路径**：一句话学习顺序——先啃哪个主干、哪个分支可暂缓。**路径编号与模块逐一对应**，不得把多个模块合并成一个编号；确需并行学习时用文字说明（如"③④可并行"），不用合并编号糊弄。
3. **推荐书单**：推荐 2-3 本**与该领域最对口**的书目或课程，规则：按相关性排序、优先领域经典；每本必须查证真实存在、附链接、一句话说清推荐理由；查证不充分的标"待验证"。并明确提示："这是初稿，请用这些书目的目录校准这张地图后，再往里面填细节。"
4. 末尾用「这张地图管什么」收尾：一句话说清覆盖范围和不覆盖什么，并附信息源清单。
5. **有温度的收尾**：最后用一两句真诚的话接住用户——扣住"你主动来学新领域"这件事给鼓励，最好呼应具体领域和场景（如"地图只是门票，进项目卡住了随时回来补局部"），拒绝万能鸡汤和长篇煽情。好的收尾是用户体验的最后一公里，让人下次还想来。

**然后生成可视化附件**（加分项，不替代文字）：呈现结构统一为「封面卡片 + 点按浮窗」——这是已验收的固定版式，不做拓扑切换，保证任何领域、任何执行者产出都稳定清晰。

**HTML 知识地图**（附件）：单文件 HTML（全部 CSS 内联、零外部依赖、双击可开、移动端可读）。**移动端适配是硬要求**：640px 以下小屏必须单栏、字号与留白收敛、禁止横向溢出，模板之外自定义的任何区块同样遵守（沿用模板 @media 规则），不得只做桌面版。保存到工作区并给用户文件链接。**区块导语必须承载结构判断**：「先学这些」的导语讲清为什么这么分、顺序和依赖/闭环关系（如"前面的不搭，后面跑不动"），「有空再学」的导语讲清为什么可以先放着、何时再碰——一句一个判断，禁止只写操作说明；卡片右上角自带「展开」标识，副标题不再重复操作提示。**优先以包内 `assets/map-template.html` 为设计模板**：保留其全部 CSS 与布局骨架，只替换 {{槽位}} 与卡片内容（卡片按需复制/删除），不要临场重写样式——这是已验收的设计水准，保证任何环境、任何执行者产出一致。**模板保底、声明式发挥**：基准样式（配色、字体、卡片、留白）不临场重发明；内容适配（主色随领域气质微调）可以发挥；确需偏离模板风格时，必须在回复里说明一句偏离理由，禁止静默漂移。内容 = 地图本体。**入口框按领域类型三选一**：职场/专业领域用「这个领域在解决什么问题」，技能/爱好领域用「这个领域在满足什么需求」，人物/文化/兴趣领域用「这是什么、为什么这么多人着迷」；三种都套得勉强时直接省略入口框，禁止硬造假问题。入口框放在标题之后、模块卡片之前，含一句话本质 + 2-3个子问题/子方向 + 与模块的对应关系。

结构上采用「封面卡片 + 浮窗弹层」：封面为紧凑卡片网格（编号+模块名+一句话+概念标签），点卡片通过纯 CSS :target 浮窗（零 JS）展开该模块的关键概念与大白话——页面保持短，详情在弹层里。要求独立可分享；检索证据链全list只放对话，HTML 底部附简版。环境不支持写文件时，退回 Mermaid 或文字层级结构图并说明。

## 边界

- 只画地图，不做逐点讲解。用户要深入某个概念时，提示另开对话或使用讲解类 skill。
- 地图是起点不是终点：画完建议用户带着地图进真实项目，卡住了再回来补局部。
- 不编造领域事实或信息源；检索后仍不确定的，标注"待验证"。



